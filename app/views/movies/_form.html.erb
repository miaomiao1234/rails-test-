<%= form_for @movie do |f| %>
<% if @movie.errors.any? %>
<ul>
  <% @movie.errors.full_messages.each do |msg| %>
  <li><%= msg %></li>
  <% end %>
</ul>
<% end %>

影片名称：
<br>
<%= f.text_field :title %>
<br>
影片描述：
<br>
<%= f.text_area :description %>
<br>

<%= f.submit "提交", :disable_with => 'Submiting...' %>
<% end %>
